package net.lksls.legendaryserver.rarity;

import net.lksls.legendaryserver.item.ModItems;
import net.minecraft.client.MinecraftClient;
import net.minecraft.item.Item;
import net.minecraft.text.Text;
import net.minecraft.util.Formatting;
import net.minecraft.util.Rarity;

public class RarityManager {

    //public static ModRarity getRarityForItem(Item item) {

        // EXAMPLES — add infinite items here
       // if (item == ModItems.LIGHT_CORE) return ModRarity.MYTHIC;
        //if (item == ModItems.DARK_CORE) return ModRarity.EXOTIC;
        //if (item == ModItems.GHOST_CORE) return ModRarity.SHINY_SACRED;
        //if (item == ModItems.DARK_SOUL_CORE) return ModRarity.SACRED;
        //if (item == ModItems.SHADOW_CORE) return ModRarity.CORRUPTED;
        //if (item == ModItems.SOUL_CORE_ARMOR_SMITHING_TEMPLATE) return ModRarity.ASTRAL;

        //return null;
    //}

    //public static boolean shouldGlow(ModRarity rarity) {
    //    return rarity == ModRarity.SACRED || rarity == ModRarity.SHINY_SACRED;
    //}

    //public static Formatting getAnimatedColor(ModRarity rarity) {
     //   long tick = MinecraftClient.getInstance().world.getTime();
    //   return rarity.getColorForTick(tick);
    //}
}
